# 更新记录

版本规则：每次功能/修复变更都递增版本号（主程序与安装器保持同步），并在本文件补充说明。

## v1.2.1（2026-08-19）

### 修复
- **局域网访问提示修正**：经实测与源码确认，**DSH 0.1.0-rc 系列出于安全限制不支持局域网绑定**
  （`--host 0.0.0.0` 被硬性拒绝，报"intentionally not supported yet for safety"，防远程代码执行；
  webserver 配置也只允许 `127.0.0.1` / `0.0.0.0`）。此前概览页「局域网访问」卡会显示一个
  实际不可访问的 `http://<本机IP>:<端口>` 地址，诊断页给出的 `--host 0.0.0.0` 命令也会启动失败，
  均属误导。现改为按 DSH 版本如实提示：
  - 概览页：「仅本机可访问」+ 原因说明（DSH 版本限制，详见诊断页）；
  - 诊断页：局域网命令区显示"暂不可用"+ 原因 + 反向代理建议；
  - 版本判断见 `DshService.SupportsLan()`（rc 系列按不支持处理，**DSH 未来放开 0.0.0.0 后
    此判断需更新**，届时管理器自动恢复局域网提示）。
- 顺带：诊断页局域网命令中的 `--trusted-host <设备IP>` 更正语义（fence 校验的是访问所用的
  authority，非客户端来源 IP；当前版本下该参数尚无用武之地）。
- **「一键升级 dsh」按钮逻辑修正**：此前只要 dsh 是 npx 缓存安装就可点击，已是最新版本时
  点它会白跑一次 npx 重装。现改为**仅检测到新版本时可点**（已最新 / 检查失败 / 版本未知时
  按钮禁用变灰），并在 `UpgradeDsh` 中加兜底提示，避免误操作。

## v1.2.0（2026-08-15）

### 新增
- **「关于」页面（新增第 5 个标签页）**：版本信息、更新检查、运行环境、文档链接与版权。
- **更新检测**：
  - 检测 **dsh** 是否有新版本（查询 npm registry，支持 0.1.0-rc.x 预发布版本比较）；
  - 检测**管理器自身**是否有新版本（查询 GitHub Releases）；
  - 启动时静默检查一次，发现更新弹托盘气泡；「关于」页可手动检查；
  - 「一键升级 dsh」：npx 缓存安装的 dsh 可直接在线升级（实时进度、超时保护）；
  - 版本比较用数字段 semver（rc.9 与 rc.10 不会比错）。
- **管理器半自动自更新**：检测到新版本 → 下载便携包（显示进度）→ SHA-256 校验（校验文件存在时）→ 解压 → 退出后由 update.bat 自动替换 exe 并**自动重启**；
  - **安装版**（注册表安装）不做自替换，引导重新运行 Setup 安装向导升级，避免破坏安装器与卸载入口；
  - 发布流程（release.ps1）自动生成并上传 `checksums.txt`（两个安装包的 SHA-256）。

### 修复
- **更新检测在 .NET Framework 4.8 下失败**：`HttpWebRequest` 默认 TLS 协商版本过低，
  npm registry / GitHub API 均要求 TLS 1.2+，报"未能创建 SSL/TLS 安全通道"。
  修复：`UpdateService.HttpGet` 中显式 `ServicePointManager.SecurityProtocol |= Tls12`。
  **后续改进注意**：本程序运行在 .NET Framework 4.8（csc v4.0.30319），凡访问外网
  HTTPS 接口必须显式启用 TLS 1.2，否则会静默失败（PowerShell 7 无此问题，易误导排查）。
- **关于页文字/按钮灰色底纹**：根因有两层——
  ① 原生 Label / PictureBox 默认 BackColor 是系统灰（SystemColors.Control），必须显式
  设 `Color.Transparent`；② 若在页面上用 Surface 色绘制"卡片"背景，卡片内的自绘控件
  （擦 Page 底色）与透明 Label（透出 Page 底色）都会与卡片色产生色差，形成底纹块；
  曾尝试用 CardPanel 容器让子控件透出卡片色，但 Surface 是半透明色，二次叠加后色差
  反而更明显。
  **稳妥处理方式（后续同类型问题照此办）**：分组区域**不要填充 Surface 色**——控件
  直接放在页面上（统一页面底色），分组区域只画圆角边框（`Draw.StrokeRound`），任何
  主题下都不会出现底纹（见 `Manager.cs` BuildAbout 顶部注释）。
- **关于页布局细节**：版本值文字与标签垂直对齐（同用 SectionLabel 同字号）；链接按钮
  之间保留 12px 间距；"打开日志目录"按钮与运行环境文字不重叠。

## v1.1.1（2026-08-15）

### 修复
- **高 DPI 标签栏被遮挡**：2560×1600 / 200% 缩放等高分屏下，概览/日志/设置/诊断四个标签
  按钮下半被下方页面遮住——标签栏高度未乘 DPI 缩放系数，现已修正。
- **诊断页标签文字灰色底纹**：诊断页「node.exe（可选）/ dsh 入口（可选）」两行文字
  标签（原生 Label）默认背景是系统灰（SystemColors.Control），深色模式下显灰色块；
  已改为透明背景（透出页面底色），并在主题切换逻辑中强制所有 Label 保持透明，
  防止同类问题复发。诊断页两个路径输入框背景同时改为与页面底色完全一致。
  **后续改进注意**：原生 Label 默认 BackColor 是系统灰，创建时必须显式设为
  `Color.Transparent`（见 `Manager.cs` BuildDiag 中 lNode/lDsh 的注释）。

### 优化
- **「一键安装 dsh」实时进度**：安装过程不再静默——npx 的 stdout/stderr 逐行实时
  显示在诊断框，按钮同步显示已用秒数（安装中 12s…），安装完成/失败明确弹窗告知；
  并加 10 分钟整体超时（网络卡死时自动终止安装进程并复位界面，不会永久卡住）、
  流式进程句柄显式释放、完成回调防重入。
- **启动成功提示访问地址**：点击「启动」成功后，若配置了自动打开浏览器则打开并在
  托盘弹出提示（含访问地址）；未开启自动打开时，托盘提示告知在「概览」页点击
  「打开浏览器」即可使用。看门狗静默自动重启不打扰。

## v1.1.0（2026-08-14）

### 新增
- **兼容任意 dsh 安装方式**：支持从源码仓库（git clone deepseek-ai/DeepSeek-Harness）、
  绿色包等非标准方式运行的 DeepSeek Harness——管理器从正在运行的实例自动反向发现其
  node 与 dsh 路径并记住（持久化到配置），停止后重启用原环境拉起。
- **node 与 dsh 成对匹配**：发现到的组合优先于单点查找，避免"仓库 dsh 配自带 node"错配。
- 诊断页新增：**手动指定 node.exe / dsh 入口路径**（浏览选择并保存）；「一键安装 dsh」按钮。
- **安装器升级匹配**：重跑安装包自动预填上次安装目录（识别为"升级"，保留配置与日志）；
  程序运行中安装会提示先退出。
- 安装包/主程序补齐**文件属性元数据**（描述、文件版本、产品名称、产品版本）。

### 修复
- **看门狗弹窗死循环**：启动失败（如环境缺失）时不再反复自动重试并弹窗；
  看门狗仅做"崩溃恢复"（最近成功运行过又意外退出才自动重启，连续失败 3 次停止）。
- **错误信息区分**：明确提示"未找到 node.exe"或"未找到 dsh"+ 处理指引，不再笼统报错。
- CLI 模式未加载配置（导致持久化路径在 CLI 下失效）。
- 环境信息卡默认显示实际生效的 DSH_HOME 目录（未设置时显示默认路径）。

## v1.0.0（2026-08-14）

### 首个正式版
- 桌面管理器：服务状态总览、启动/停止/重启、多实例管理、日志查看器、看门狗、托盘常驻。
- 浅色/深色/跟随系统三种主题，平滑过渡动画（透明度呼吸切换），标题栏即时变色。
- 自检诊断、局域网访问提示、开机自启、自动清理 30 天前旧日志。
- 随包分发便携版 Node.js（对方无需安装 Node）；单文件 exe，无第三方依赖。
- 两种分发形式：安装版（NSIS 风格单文件 Setup，含安装/升级/卸载）与便携免安装版 zip。
